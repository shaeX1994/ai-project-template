# example-module

示例模块文档。`ai-init --drop-examples` 会删掉它。每个模块一份,由改动该模块的 agent 维护。

## 职责

一句话说明这个模块负责什么,以及它明确不负责什么。

## 边界

- 对外提供:<接口、事件、导出的类型>
- 依赖:<模块或服务,以及依赖的是哪个契约>
- 不允许:<明确禁止的依赖方向或访问方式>

## 不变量

- <必须永远成立的条件>

## 关键决策

- 引用 `docs/decisions/` 里的 ADR,不在这里重复论证。

## 变更须知

改这个模块时必须注意的事:哪些测试要跑、哪些配置要同步、哪些调用方会受影响。

<!--
维护规则见 .ai/core/documentation.md:
只更新本次变更真正作废的小节,其余部分逐字节保持不变。
-->
